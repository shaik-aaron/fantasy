export type LoginDetails = {
    email: string,
    password: string
}

export type SignUpDetails = {
    firstName: string,
    lastName: string,
    email: string,
    password: string
}

export type userDetails = {
    userId: number,
    firstName: string,
    email: string
}